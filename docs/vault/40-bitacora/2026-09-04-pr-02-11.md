---
fecha: 2026-09-04
fase: "02"
agente: claude-opus-5
---

## Tareas cerradas

- **T-02-019** — RED: `session_test.go`, once funciones (549 líneas)
- **T-02-020** — GREEN: `session.go` (360 líneas) + dos queries nuevas (21)

Cierra el trabajo de **PR-02-11**. Rama `feat/pr-02-11-session` sobre
`feat/pr-02-10-recovery`. **No mergeable todavía: lo gatea Judgment Day (`T-02-021`).**

## El hallazgo del día: un rechazo que escribe no puede viajar como error

Dos idiomas, los dos correctos por separado:

1. **Go** — un rechazo se comunica devolviendo un `error`.
2. **Transacciones** — `db.WithAuthUser` hace **rollback** cuando el callback devuelve error
   (`internal/db/auth.go:68`, `:77`).

Juntos rompen el único caso donde **un rechazo tiene que escribir**.

La detección de reutilización es exactamente ese caso: si el token presentado ya fue rotado,
se revoca **toda su familia** y se rechaza la petición.

```go
// La primera versión, y estaba mal:
if presented.RevokedAt.Valid {
    queries.RevokeRefreshTokenFamily(ctx, presented.FamilyID)  // escribe
    return ErrRefreshTokenReused                               // ...y el rollback la borra
}
```

El error sube, `WithAuthUser` hace rollback, y **la revocación se deshace**. El ladrón recibe
un 401 y **se queda con la sesión viva** — el resultado exacto que la revocación de familia
existe para impedir.

Y no falla nada. No hay excepción, no hay log, la función "funciona": rechaza. Solo que la
contención nunca ocurrió.

### El arreglo es de contrato, no un parche

```go
type RotationOutcome struct {
    Cookie  string  // vacío salvo éxito
    Refusal error   // por qué es 401, o nil
}

func RotateRefreshToken(...) (RotationOutcome, error)  // el error es SOLO infraestructura
```

El llamador devuelve al callback **únicamente** el error de infraestructura, así que el
rechazo commitea con su revocación adentro.

Con una excepción que se nombra en su propio comentario: **perder una carrera de rotación
concurrente sí usa el `error`**, porque ahí ya se insertó la fila sucesora y hay que
deshacerla. La regla no es "los rechazos nunca son errores", es **"un rechazo commitea si y
solo si dejó algo escrito que tiene que sobrevivir"**.

### Cómo apareció, que importa tanto como el qué

**Lo encontró el primer GREEN. No la mutación, no una revisión.** Y solo porque el RED había
escrito el **escenario completo del robo** —rotar, presentar la copia vieja, y después
verificar que el token que el usuario legítimo tenía en la mano quedó revocado— en vez de
limitarse a *"devuelve `ErrRefreshTokenReused`"*.

Un test que solo aserta el error habría pasado con el bug puesto. La aserción que lo mató es
la del **estado después**, y estaba ahí porque el spec la pedía como escenario propio. Ese
tercer escenario parecía redundante cuando lo escribí. No lo era.

### La otra cara del hallazgo de ayer

Un PR antes, en `recovery.go`, **el mismo rollback volvió equivalente a un mutante**: mover una
guarda debajo de un `DELETE` no cambiaba nada porque el rollback preservaba las filas. Ahí el
rollback nos salvaba.

Acá nos destruye. Es la misma capa contestando, y **no es ni amiga ni enemiga**: es una capa
que hay que tener en el modelo mental. Las dos veces la pregunta útil fue idéntica: *¿qué
sobrevive al final de esta transacción, y quién lo decidió?*

## Mutación — seis mutantes, seis muertos

El DoD exigía que **todo** mutante sobre `WHERE family_id = $1 AND revoked_at IS NULL` muriera.

| Mutante | Qué murió |
|---|---|
| `WHERE family_id` → `WHERE id` (revoca solo el presentado) | los dos tests de familia |
| `revoked_at IS NULL` → `IS NOT NULL` | los dos |
| la reutilización devuelta como error de infraestructura — **el bug original** | los dos |
| la rotación arranca una familia nueva | tres, incluido el de rotación feliz |
| chequeo de expiración quitado | el de expiración, y solo ese |
| rama de reutilización quitada entera | los dos |

El tercero es el bug original convertido en mutante permanente: si alguien "simplifica" la
firma rara de `RotateRefreshToken`, ese mutante lo espera.

## Tres decisiones de la implementación

- **`RotateRefreshToken` recibe el secreto, nunca el `user_id` parseado.** El prefijo es input
  controlado por el atacante; su único trabajo es decirle al *llamador* qué scope abrir. El
  dueño del token nuevo sale de la fila que devolvió la base dentro de ese scope — el único
  `user_id` que ningún cliente eligió.
- **`ErrRefreshTokenReused` envuelve a `ErrRefreshTokenInvalid`.** La reutilización es un
  evento que el servidor tiene que poder alarmar aparte, pero la respuesta HTTP es idéntica.
  Con el wrap, ningún llamador puede tratarla como éxito.
- **La expiración no quema la familia.** Una sesión que simplemente quedó quieta no es
  evidencia de robo; quemarla desloguearía todos los dispositivos y dispararía la alarma sobre
  un no-evento.

## Lo que se fija con precisión incómoda

La garantía **no es** "detectamos el robo". Es **"el robo no sobrevive al próximo refresh del
usuario legítimo"**: quien refresque segundo es rechazado, y los dos quedan deslogueados. Es
más débil que detección y más fuerte que nada, y está escrito en la cabecera del archivo para
que nadie lo lea como lo primero.

## Presupuesto — excede los dos, otra vez

| | implementación | total |
|---|---:|---:|
| límite | 250 | 800 |
| medido | **381** | **930** |
| exceso | **+131** | **+130** |
| `est:` / proyección | 320 | 755 |

`sqlcgen` queda excluido de los dos por la regla del `est:` (línea 56 de `tasks.md`); las 21
líneas de `query/auth.sql` sí cuentan.

**Necesita `size:exception`.** Y mi predicción volvió a quedarse corta: al cerrar `PR-02-10`
dije que esto rondaría 700-750 "probablemente adentro". Midió 930. Acerté en la dirección —
dije que la proyección de 755 se sostenía porque acá hay superficie adversarial pura— y erré
la magnitud, que es el mismo error de la vez pasada con el signo cambiado.

## Deuda que NO se arregló acá, a propósito

`internal/db/auth.go` y `auth_test.go` están **sucios de `gofmt`**, preexistentes de `6bffd3c`
(T-02-002) y sin tocar por este PR. No se arreglan acá porque **el diff de este PR es
exactamente lo que Judgment Day revisa**, y ensancharlo con archivos ajenos empeora esa
revisión. Queda anotado en `T-02-020` y va a `T-02-021`, que ya tiene `auth.go` en su alcance.

## Verificación

- `-run 'Refresh' -v` — las **once** funciones confirmadas por nombre, contenedor incluido
- suite completa verde
- `golangci-lint run ./internal/auth/ ./internal/db/` — 0 issues
- `govulncheck ./...` — limpio (la única de módulo sigue siendo `openpgp`, preexistente)
- residuo de mutación: verificado con `rg`, cero

> **Nota de herramienta:** `go vet` en el host se colgó (dos minutos sin salida) y hubo que
> usar el contenedor. Es el problema conocido de Smart App Control en este equipo. El runner
> válido de este proyecto sigue siendo `make test-api-container`.

## Siguiente

**`T-02-021` — Judgment Day.** Revisión adversarial ciega de `session.go` junto con la ruta de
acceso a `refresh_tokens` (`auth.go`, `00013`) como una sola unidad, **antes** del merge de
`PR-02-11`. Detrás esperan 13 PRs.
