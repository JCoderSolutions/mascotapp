---
project: mascotapp
current_phase: "02"
current_task: "T-02-005"  # T-02-016 (PR-02-04) cerrada en local, branch feat/pr-02-04-totp-recovery-codes; no pusheado, sin PR abierta
sdd_change: "phase-02-auth-and-multitenancy"
rdd_enabled: false  # fase marcada RDD; el usuario decide, no se activa solo
task_status: in_progress  # PR-02-01..PR-02-04 verdes en local. Sigue T-02-005 (PR-02-05, slice b)
blocked_by: ".env.example: denegado a los agentes por regla global. Bloquea SOLO T-02-035 (PR-02-24, ultimo de la cadena). El usuario tiene que confirmar si JWT_SECRET y RESEND_API_KEY ya existen y pegar DATABASE_URL_AUTH, AUTH_KEK, WEB_ORIGINS, API_PUBLIC_ORIGIN y el password de app_auth. La cadena funcional entera mergea sin eso."
last_updated: 2026-09-03
sdd_store: hybrid
last_agent: claude-opus-5
next_action: "ARRANCA CON `make doctor`. Es nuevo (2026-09-03) y es el paso 0 del ritual: ejecuta -- no solo busca en el PATH -- cada herramienta externa del flujo y dice que se rompe sin cada una. Sale distinto de cero SOLO si falta algo requerido. NUNCA lo pases por un pipe: existe precisamente porque un pipeline se comio un `command not found`. Vive en scripts/doctor.go (//go:build ignore, corre por `cd apps/api && go run`, igual que engram-index.go, porque TestDevcontainerInstallsEveryToolTheMakefileInvokes deriva su lista de instalados solo de lineas `go install ...@ver`). Tiene un backstop anti-deriva probado por mutacion: si un recipe del Makefile invoca una herramienta que el manifiesto no lista, doctor falla contra si mismo en vez de quedarse callado y desactualizado. NO chequea `rg` a proposito -- `rg` es una FUNCION DE SHELL que inyecta Claude Code, no hay binario en el PATH y nunca lo va a haber, asi que ese chequeo estaria rojo para siempre en una maquina sana y entrenaria al lector a saltearse las lineas WARN, que es el habito exacto que este programa existe para romper. ESTADO DE LAS HERRAMIENTAS AL 2026-09-03 (salida real de make doctor): go 1.27.0, docker 29.7.2, git, npm, golangci-lint 2.13.2, govulncheck, sqlc v1.31.1 y oapi-codegen en OK; `fresh-binary` en OK -- o sea que HOY este host compila y ejecuta un binario recien linkeado, lo que NO significa que Smart App Control se haya ido: es una observacion de una corrida, no una conclusion, y el runner sigue siendo make test-api-container hasta que una suite real diga otra cosa. En WARN: `goose` (no esta en el host; solo afecta migrate/migrate-down/db-reset contra una base real, la suite embebe las migraciones y las aplica sola) y `gentle-ai`. GENTLE-AI REINSTALADO EL 2026-09-03 A PEDIDO DEL USUARIO: `go install github.com/gentleman-programming/gentle-ai/cmd/gentle-ai@v1.49.0`. GOPROXY=off NO alcanza -- el zip de gentle-ai esta en cache pero dos dependencias transitivas (golang.org/x/sys v0.38.0 y golang.org/x/text v0.3.8) no, asi que hace falta red; la instalacion por red funciono sin problemas de TLS en el host (el problema de `certificate signed by unknown authority` es de la red DEL CONTENEDOR, no del host). Verificado POR EJECUCION, no por LookPath: make doctor reporta `OK gentle-ai gentle-ai 1.49.0`. LEDGER SDD SANO: `gentle-ai sdd-status` da next=apply, apply=ready, blockedReasons=[], tasks 5/39. TRES CORRECCIONES A LO QUE ESTA NOTA DECIA ANTES, todas verificadas contra el binario real: (1) NO EXISTE `gentle-ai sdd-attempt` en 1.49.0 -- el comando que estas notas y los skills de ~/.claude nombran es una superficie de 2.x. El registro del intento vive en .git/gentle-ai/sdd-runtime/v1/<change>/records/*.json y su HEAD sigue siendo el `objective/advance` de pr-02-04-acquire-001 (max_attempts 30, max_changed_lines 7000, ordinal 4) sin record de settle detras. NO BLOQUEA NADA: el dispatcher dice apply ready. (2) TAMPOCO EXISTE el comando `review` en 1.49.0 -- ni `review status --contract gentle-ai.review-integration/v2 --next-transition` ni `review mode enable/disable`. Todo el contrato RDD del CLAUDE.md global necesita 2.x. RDD sigue apagado y es decision del usuario, asi que no bloquea, pero si alguna vez se enciende hay que subir de version primero. (3) 1.49.0 ESTA TRES MAJORS ATRAS: `gentle-ai update` reporta latest 2.5.0. Se instalo 1.49.0 a proposito, que es lo que habia en el module cache y lo que el proyecto venia usando; SUBIR A 2.5.0 ES DECISION DEL USUARIO Y TIENE RIESGO REAL A MITAD DE FASE -- el store del runtime esta bajo un directorio `v1/` y un binario 2.x podria no leer esa historia; ademas el instalador publicado es un one-liner de PowerShell `irm ... | iex`, que cae en la lista deny (curl-a-shell) y es una accion de cadena de suministro. Contexto historico de la desaparicion, verificado el 2026-09-03: `command not found`, no esta en el global de npm (solo claude-code, codegraph, eas-cli, opencode-ai), no es plugin de Claude (installed_plugins.json y known_marketplaces.json no lo mencionan; los skills y agents sdd-*/review-*/jd-* siguen instalados, lo que falta es el BINARIO). CAUSA DE INSTALACION IDENTIFICADA ESTA SESION, dato nuevo: se instalo por `go install`. El module cache tiene github.com/gentleman-programming/gentle-ai@v1.49.0 con su zip completo, y GOBIN es C:\\Users\\Jose\\go\\bin, donde estan golangci-lint, govulncheck, oapi-codegen y sqlc pero NO gentle-ai.exe. REINSTALACION (decision del usuario, no del agente; `go install` esta en la lista ask): `go install github.com/gentleman-programming/gentle-ai/cmd/gentle-ai@v1.49.0` -- y como el zip ya esta en cache conviene GOFLAGS=-mod=mod GOPROXY=off para no depender de la red, que en este host intercepta TLS y rompe descargas con `certificate signed by unknown authority`. CONSECUENCIA QUE NO BLOQUEA: el intento SDD de PR-02-04 quedo sin settle (record HEAD sha256:4c12c952548826e07947d3bfcbde6b87cb54b78fb32b88658a83fd432bb9f6b6, operation objective/advance, request_id pr-02-04-acquire-001). El dispatcher no lo trata como bloqueo. Cualquier reset del ledger sigue requiriendo decision explicita del mantenedor y nunca es automatico. CADENA DE RAMAS AL 2026-09-03: main 4677653 (9 commits) -> feature/fase-02 9c577fd -> feat/pr-02-01-auth-door 1f12b20 (399 lineas) -> feat/pr-02-02-auth-role-migration e32b116 (579, size:exception) -> feat/pr-02-03-auth-queries 42a0dbe (314) -> feat/pr-02-04-totp-recovery-codes 7be8200 (280) -> chore/make-doctor (HEAD actual). LA RAMA chore/make-doctor NO ES UNO DE LOS 24 PRs: es el nodo de herramienta donde vive make doctor, y esta separada A PROPOSITO. Meterla en feat/pr-02-04 habria inflado despues de cerrado un PR ya reportado en 280 lineas; el mismo criterio con el que el generador de engram salio del PR-02-01. PR-02-05 SE RAMIFICA DE chore/make-doctor, no de feat/pr-02-04, para que la proxima sesion tenga el preflight desde el paso 0. NADA PUSHEADO, NINGUNA PR ABIERTA -- lo gatea el usuario. SESGO DE ESTIMACION 4 DE 4 HACIA ARRIBA, NUNCA HACIA ABAJO: 250->399, 260->579, 150->314, 160->280; la rebanada (c) esta planificada en 3.140 lineas, leelo antes de creerle a un estimado. SIGUE PR-02-05 = T-02-005 + T-02-006, est 270, en rama feat/pr-02-05-column-grants sobre chore/make-doctor: pin de la semantica de privilegio por columna (self-contained contra tabla/rol de scratch) + migracion 00015_column_grants (B1) + la correccion del comentario obsoleto de TestApplicantPolicy_IsAsWideAsWritingAnApplication. RECORDA EL GOTCHA DE LA REBANADA (b): una denegacion por privilegio de columna dice `permission denied for TABLE`, no `column` -- se asierta el SQLSTATE 42501, NUNCA el texto. ||| HISTORIAL ANTERIOR ||| PR-02-04 (T-02-016) CERRADO EN LOCAL el 2026-09-02, rama feat/pr-02-04-totp-recovery-codes sobre feat/pr-02-03-auth-queries: migracion 00014_totp_recovery_codes.sql (tabla, RLS ENABLE+FORCE y la policy auth_own_recovery_codes en la MISMA migracion que la tabla -- sin entrada en PolicyLandsAt, tal como pedia la tarea), totp_recovery_codes entra a rlstest.Schema.NonTenantModel (4->5 no-tenant, 19->20 model), query/auth.sql suma InsertRecoveryCode/DeleteRecoveryCodesForUser/GetRecoveryCodeByHash/RedeemRecoveryCode, y los dos escenarios de data-model-core (codigo hasheado, segunda redencion rechazada) como tests de integracion nuevos en totp_recovery_codes_test.go. RED confirmado contra el contenedor real ANTES de escribir la migracion: TestCatalog_EveryRelationIsClassifiedAndProtected fallaba con 'totp_recovery_codes... does not exist', y los dos tests nuevos con SQLSTATE 42P01. UN HALLAZGO NO PREVISTO POR LA TAREA: un TERCER test fijado, TestTenancyPolicies_ApplyToTheRightRoleAndCommand (isolation_test.go), tiene hardcodeado el inventario completo de policies y privilegios de todo el esquema y necesito filas nuevas para totp_recovery_codes (una policy, seis privilegios) -- la misma convencion 'cada migracion suma sus filas a las dos tablas de abajo' que refresh_tokens siguio en 00013; el catalogo cuenta policies pero no ve DE QUIEN son. Suite completa (make test-api-container) verde, TestMigrations_EveryStepDownLeavesAConsistentSchema incluido (walked 14 migrations, 170 tablas inspeccionadas). make generate: diff limpio, solo sqlcgen/auth.sql.go, models.go, querier.go. golangci-lint: 0 hallazgos nuevos (el gosec G101 preexistente en dbtest/container.go, no tocado). Medido: 280 lineas autoradas contra el estimado de 160 -- dentro del presupuesto de 400 por PR, sin size:exception. SIGUE T-02-005 (PR-02-05: pin de la semantica de privilegio por columna que la rebanada (b) necesita, self-contained contra una tabla/rol de scratch, est 55). PR-02-03 (T-02-004) CERRADO EN LOCAL el 2026-09-02, rama feat/pr-02-03-auth-queries sobre feat/pr-02-02-auth-role-migration: refresh_tokens sale de NoPolicy en rlstest/catalog.go, query/auth.sql (GetRefreshTokenByHash, RevokeRefreshTokenFamily, GetUserCredentialsByEmail, ListOwnMemberships), TestPolicies_DoNotCrossGUCs (P2-D1, escanea pg_policy via pg_get_expr). UN HALLAZGO NO PREVISTO POR LA TAREA: sacar refresh_tokens de NoPolicy rompia un SEGUNDO test ademas del previsto en tasks.md -- TestMigrations_EveryStepDownLeavesAConsistentSchema, porque el walk stepwise inspecciona el esquema en CADA version intermedia de la migracion, y entre 00002 y 00012 refresh_tokens tiene RLS encendida con CERO policies de verdad -- 00013 es la que le da auth_own_sessions. CheckProtection es una regla unica sin nocion de version, asi que al sacar refresh_tokens de NoPolicy el walk leyo ese hueco historico real como un rollback roto. Arreglado con Classification.PolicyLandsAt (map[string]int64, mismo patron que Pending pero para un hueco distinto: Pending es 'la tabla no existe todavia', esto es 'la tabla existe, la policy llega en una migracion posterior') y un nuevo metodo CheckProtectionAt(t, at) que el walk llama en vez de CheckProtection; CheckProtection sin version sigue exactamente igual de estricta en HEAD, no se toco su contrato. Los conteos NO se movieron: 4 no-tenant / 19 model, exactamente como pedia la tarea. TODOS los tests nombrados confirmados en verde por -run -v (no por 'ok' del paquete): TestQueries_ExerciseEveryDeclaredTable, TestSchema_MatchesTheDeclaredCounts, TestCatalog_EveryRelationIsClassifiedAndProtected (19 de 19 verificadas), TestMigrations_EveryStepDownLeavesAConsistentSchema (walked 13 migration(s); inspected 150 model table(s)), TestPolicies_DoNotCrossGUCs, TestPolicyRow_Mentions (4 subcasos). make generate: diff limpio, solo sqlcgen/auth.sql.go (nuevo) y querier.go; oapi-codegen y npm run generate sin diff porque no se toco el contrato HTTP. golangci-lint run ./...: 0 hallazgos nuevos -- el unico (gosec G101 en dbtest/container.go:67) es preexistente y ese archivo no se toco en este PR. SIGUE T-02-005 (PR-02-05: pin de la semantica de privilegio por columna que la rebanada (b) necesita, self-contained contra una tabla/rol de scratch, est 55). PR-02-02 (T-02-003) CERRADO EN LOCAL el 2026-09-02, rama feat/pr-02-02-auth-role-migration: migracion 00013_auth_role (rol app_auth, cinco policies, grants por columna), tercer pool en dbtest, seis casos de comportamiento y el inventario de policies. Suite completa verde; los seis tests confirmados por nombre con -run -v. SIZE:EXCEPTION ACEPTADA POR EL USUARIO: 579 lineas contra un estimado de 260. El estimado estaba mal, no el trabajo; el unico corte honesto daba 407/172 y separaba un cambio de seguridad de su prueba por un PR a cambio de siete lineas. Aplica SOLO a PR-02-02. TRES HALLAZGOS DE T-02-003. (1) HAS_TABLE_PRIVILEGE NO VE LOS GRANTS POR COLUMNA, verificado en vivo contra PG 17: GRANT SELECT (a) ON t deja has_table_privilege en false y has_column_privilege(a) en true. Cuatro filas del inventario esperaban true sobre grants por columna. Corregidas a false, que es la asercion MAS FUERTE porque se pone roja si alguien ensancha el grant a la tabla entera; refresh_tokens sigue en true porque ahi el grant si es de tabla. (2) FALTABA auth_own_memberships en la lista de policies esperadas mientras un test exigia alcance por fila sobre memberships: cualquiera de los dos caminos fallaba. (3) DOS timestamptz se scaneaban a *string; la lectura pasaba y fallaba el scan. EL AGENTE SE CAYO POR LIMITE DE SESION con el RED escrito y GREEN sin empezar, y su ultimo mensaje proponia reordenar los auth_* antes de applicant_visible_users: la query ordena COLLATE C y bajo bytes applicant < auth < member, asi que la ubicacion que ya tenia era la correcta y el corte lo salvo de romperla. GREEN lo escribio el orquestador inline. SIGUE T-02-004 (PR-02-03: clasificacion de catalogo + query/auth.sql + TestPolicies_DoNotCrossGUCs, est 150). PR-02-01 (T-02-001 RED, T-02-002 GREEN) CERRADO EN LOCAL el 2026-09-02, rama feat/pr-02-01-auth-door sobre feature/fase-02: WithAuthUser/WithAuthLookup/ErrNoAuthUser/NewAuthPool en internal/db/auth.go, mutation-tested per DoD (uuid.Nil check, is_local flag, panic re-raise, rollback-on-error, no-SQL-interpolation), reusando los stubs recordingTx/recordingPool/recordingTxPool de tenant_test.go/db_test.go. make test-api-container EN VERDE, suite completa, y los ocho tests de auth verificados por nombre con -run/-v (no por 'ok' del paquete, que no prueba que corrieran). MUTACION: cuatro mutantes, cuatro muertos, uno por corrida y una sola linea viva cada vez -- is_local true->false, guarda uuid.Nil neutralizada, READ ONLY eliminado, y el re-panic tragado. golangci-lint run ./...: 0 issues. UNA CORRECCION AL REPORTE DEL AGENTE: dijo que el fallo de TestDevcontainerInstallsEveryToolTheMakefileInvokes era preexistente de Fase 01 y NO LO ERA -- lo introdujo el orquestador ese mismo dia al agregar 'python scripts/engram-index.py' al Makefile. El guard tenia razon: construye su lista de instalados SOLO desde lineas 'go install ...@ver', asi que un interprete no puede satisfacerlo nunca y el target se rompia en cualquier devcontainer limpio. Arreglado reescribiendo el generador en Go (scripts/engram-index.go, //go:build ignore, corriendo por go run), NO ensanchando el guard; la paridad se probo diffeando la salida contra la version Python commiteada: byte-identica, 76 entradas. LO QUE SI ESTA ROJO Y ES PREEXISTENTE, NO DE ESTE PR: govulncheck falla con GO-2026-6355 y GO-2026-6354 en golang.org/x/crypto@v0.55.0 (fixed in v0.56.0), alcanzadas solo por el harness de tests (dbtest/container.go:256 -> testcontainers postgres.Run -> ssh.NewClientConn). go.mod no se toco en este PR. Es puerta bloqueante en CI, asi que la cadena no mergea hasta bumpear x/crypto -- unidad de trabajo aparte, build(deps), y 'go get' esta en la lista ask. NO pusheado, SIN PR abierta -- el usuario gatea push/PR. SIGUE T-02-003 (PR-02-02: migracion 00013_auth_role + dbtest tercer pool + prueba de alcanzabilidad/aislamiento). El ciclo SDD llego hasta tasks: exploration, proposal, seis specs, design (P2-D1..P2-D11) y 40 tareas T-02-001..040 con cadena de entrega. SIGUE /sdd-apply desde T-02-001. ENTREGA DECIDIDA POR EL USUARIO EL 2026-09-02: PRs ENCADENADOS, no size:exception. Tres de las cinco rebanadas reventaban el presupuesto de 400 lineas y la (c) lo hacia por 7,85x (3.140 lineas: seis handlers, cuatro primitivas de cripto, migracion, middleware, CORS/CSRF, config y OpenAPI). Quedaron 24 PRs, 4.890 lineas, NINGUNO sobre 400 -- la suma por PR se verifico contra los est: de cada tarea, no contra la tabla, y ahi aparecio que el PR-02-05 daba 410 y estaba anunciado como 395; se partio en dos en vez de bajarle el estimado, porque bajar el numero para que entre es como un presupuesto deja de significar algo. Estrategia feature-branch-chain igual que la Fase 01: PR n se basa en PR n-1 y la cadena mergea junta. JUDGMENT DAY VA ANTES DEL MERGE DEL PR-02-11 (session.go, rotacion y deteccion de reutilizacion), revisado junto con la ruta de acceso a refresh_tokens que ya mergeo en el PR-02-02. Es el cuello de botella real: por la cadena, LOS 13 PRs SIGUIENTES estan detras de el en el orden de merge, dependan o no de su codigo. UNA DESVIACION DE LA NUMERACION DEL DISENO, forzada por un test y no por preferencia: el diseno numera 00013(a) -> 00014(c) -> 00015/00016(b) pero entrega en orden (a)->(b)->(c), y el invariante de T-01-005 (versiones ascendentes sin huecos, migrate_test.go:91) corre en LA RAMA DE CADA PR, no solo en el arbol final. La 00014 se adelanta al PR-02-04. Misma clase de movida que el T-01-007 de la Fase 01. CUATRO CORRECCIONES DEL GATEKEEPING, todas de la misma familia -- una afirmacion que nadie habia chequeado. (1) EL TEST QUE NO SE ROMPE: la exploracion, la propuesta Y el comentario del propio test decian que TestApplicantPolicy_IsAsWideAsWritingAnApplication se pone rojo cuando aterrizan los grants por columna. NO SE PONE: su mecanismo es un INSERT de app_tenant en adoption_applications, y B1 va sobre shelters y memberships, que no tocan esa tabla. Cerrarlo pediria REVOKE INSERT (applicant_user_id), que es el camino por el que un adoptante postula: flujo de Fase 05. Lo encontro sdd-design (P2-D6) y se verifico contra la fuente. EN ESTA FASE SE MUEVE UN SOLO TEST FIJADO, TestAssignment_DoesNotYetRequireAnActiveMembership, no dos. El comentario del test se corrige en T-02-006, en el mismo commit que la 00015. (2) UN REQUISITO QUE EL VERIFICADOR NO PODIA LEER: el diseno protegia shelters.storage_bytes_used -- el contador contra el que se chequea la cuota, mismo agujero que la cuota misma -- y el spec no lo nombraba, asi que sdd-verify nunca lo iba a chequear. Agregado a los dos specs delta junto con verified_at y verified_by, que son la segunda y tercera columna del agujero de auto-verificacion que abre status. (3) UNA SEMANTICA DE POSTGRESQL MARCADA [verified] QUE NUNCA SE HABIA CORRIDO: verificada en vivo contra PG 17 el 2026-09-02. Omitir una columna con DEFAULT no necesita privilegio (por eso el registro funciona sin poder escribir status), nombrarla da 42501, y un UPDATE de columna no concedida da lo mismo. GOTCHA QUE SE LLEVA UN TEST POR DELANTE: el mensaje dice 'permission denied for TABLE', no 'column' -- se asierta el SQLSTATE, NUNCA el texto, y eso aplica a toda asercion de 42501 de la rebanada (b). (4) EL PR QUE NO SUMABA: ver arriba. PENDIENTE DEL USUARIO Y ES LO UNICO BLOQUEADO: .env.example esta denegado a los agentes por regla global. Falta confirmar si JWT_SECRET y RESEND_API_KEY ya existen, y que el usuario pegue DATABASE_URL_AUTH, AUTH_KEK (base64 32 bytes), JWT_SECRET (>=32 bytes), WEB_ORIGINS, API_PUBLIC_ORIGIN y la variable de password de app_auth. RESEND_API_KEY NO hace falta esta fase: el stub no toca la red. BLOQUEA UNA SOLA TAREA (T-02-035, PR-02-24, ultimo de la cadena) Y NADA MAS: el harness de tests genera su propio password para app_auth por el mismo camino que uso la Fase 01, y la validacion de config.go se testea con t.Setenv. La cadena funcional entera mergea y se ejercita en CI con ese PR todavia abierto. OTROS PENDIENTES DEL USUARIO, NINGUNO BLOQUEA: T-00-018 (instalar scripts/hook-canary.ps1 a mano) y T-00-020 (billing GCP + alerta de presupuesto en USD 0). RDD SIGUE APAGADO. La Fase 02 esta marcada como fase de alto riesgo en el plan; encenderlo es gentle-ai review mode enable --scope clone y es decision del usuario, nunca del agente."
---

# Estado del proyecto

## Resumen en una línea

**Fase 02 (Auth y multi-tenancy) EN CURSO**: `T-02-001`…`T-02-004` y `T-02-016` cerradas en local (PR-02-01…PR-02-04), ninguno pusheado ni con PR abierta — eso lo gatea el usuario. `T-02-016` (migración `00014_totp_recovery_codes`) se entregó en `PR-02-04`, adelantada de la rebanada (c) por el invariante de numeración de migraciones — ver la nota de la sección **Entrega** abajo. **40 tareas** `T-02-001`…`040`, entregadas en **24 PRs encadenados** (4.890 líneas, ninguno sobre 400) por decisión del usuario del 2026-09-02. Judgment Day va antes del merge del `PR-02-11` (`session.go`) y los 13 PRs siguientes están detrás de él. Sigue `/sdd-apply` desde `T-02-005` (`PR-02-05`, rebanada b). **Arrancá con `make doctor`** — preflight nuevo del 2026-09-03, paso 0 del ritual. `gentle-ai` **fue reinstalado el 2026-09-03** (v1.49.0) y el dispatcher SDD está sano (`apply: ready`). Queda un solo `WARN`: `goose`, que no bloquea nada del trabajo. **Pendiente de decisión del usuario:** `gentle-ai` está tres majors atrás (latest 2.5.0) — sin `review` ni `sdd-attempt`; irrelevante mientras RDD siga apagado. **Fase 01 CERRADA Y ARCHIVADA** (35/35). Fase 00 queda en 21/23 con 2 tareas del usuario que no bloquean nada.

## Invariantes

1. **Como máximo UNA tarea en estado `[~]`** en todo el tablero. Si encontrás dos, es un error: resolvelo antes de trabajar.
2. **Ninguna tarea pasa a `[x]`** sin test en verde, lint limpio y `PROJECT_STATE.md` actualizado.
3. **El avance vive acá, no en Engram.** Engram guarda *por qué*, nunca *qué se hizo*.
4. **Los artefactos técnicos se escriben en inglés** (código, identificadores, columnas, endpoints, commits, tests). La documentación del vault va en español.

## Ritual de inicio de sesión — obligatorio

0. **`make doctor`** — antes de leer nada. Ejecuta cada herramienta externa del
   flujo (no solo la busca en el `PATH`) y dice qué se rompe sin cada una.
   **Leé su salida entera; nunca la pases por un pipe.** Si es tu primera sesión
   con este repositorio, leer también [AGENTS.md](AGENTS.md).
1. Leer este archivo.
2. `mem_context` + `mem_search` sobre `current_phase`. **Si tu herramienta no
   tiene el MCP de Engram** (Kiro, OpenCode), reemplazar este paso por
   [docs/vault/20-arquitectura/indice-engram.md](docs/vault/20-arquitectura/indice-engram.md):
   el texto completo de cada decisión está versionado en `.engram/queue/`.
3. Abrir `docs/vault/30-fases/FASE-<current_phase>.md`. Buscar el primer `[~]`; si no hay, el primer `[ ]`.
4. Leer el spec enlazado y la última entrada de `docs/vault/40-bitacora/`.
5. Confirmar la tarea al usuario en una línea. **Entonces** empezar.

## Ritual de cierre de tarea — obligatorio

1. Tests en verde, lint limpio.
2. Marcar `[x]`; mover el `[~]` a la siguiente tarea.
3. Actualizar este archivo, incluido `next_action`.
4. Añadir entrada a `docs/vault/40-bitacora/<fecha>.md`.
5. Evaluar candidatos de memoria contra `.engram/RUBRIC.md` → escribir a `.engram/queue/` → `make engram-index`.
6. Commit convencional: `feat(<area>): T-00-005 <descripción en inglés>`.
   Convención completa: [docs/vault/99-plantillas/plantilla-commit.md](docs/vault/99-plantillas/plantilla-commit.md).

## Enlaces

- **Contrato de trabajo para cualquier agente: [AGENTS.md](AGENTS.md)**
- Plan maestro: [docs/vault/10-propuesta/analisis-y-plan.md](docs/vault/10-propuesta/analisis-y-plan.md)
- Tablero de la fase actual: [docs/vault/30-fases/FASE-02.md](docs/vault/30-fases/FASE-02.md)
- Change SDD activo: [openspec/changes/phase-02-auth-and-multitenancy/](openspec/changes/phase-02-auth-and-multitenancy/)
- Decisiones: [docs/vault/20-arquitectura/](docs/vault/20-arquitectura/)
- Índice de memoria (sin MCP): [docs/vault/20-arquitectura/indice-engram.md](docs/vault/20-arquitectura/indice-engram.md)
- Rúbrica de memoria: [.engram/RUBRIC.md](.engram/RUBRIC.md)
